#!/bin/sh
# فحص الأسرار قبل أي commit — الريبو عام
node scripts/check-secrets.js --staged || exit 1
# حارس سقف الـ1000 صف — يمنع نداء بيتقص من غير ما حد ياخد باله
node scripts/check-fetch-limits.js || {
  echo ""
  echo "⛔ الكوميت اتوقف: فيه نداء معرّض للقص عند 1000 صف (شوف فوق)."
  echo "   لو النداء آمن فعلًا، حطّ على السطر تعليق:  // data-ok: السبب"
  exit 1
}
